@using SurfsUpv3.Models;
@model Booking;



<div style="font-family: Arial, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px; border: 1px solid #ddd; background-color:rgba(0, 0, 0, 00); border:none; box-shadow:none;">
    <h2 style="color: #2e86c1; text-align: center;">Tak for din booking</h2>
    <p>Hej @Model.CustomerName.<br /><br />Dit <strong>ordrenummer</strong> er : @Model.BookingId<br /><br /> Din reservation af surf-udstyr er bekræftet. Vi glæder os til at byde dig velkommen til en fantastisk dag på vandet.</p>

    <h3 style="color: #2e86c1;">Detaljer om din booking:</h3>
    <ul style="list-style-type: none; padding: 0;">
        <li><strong>Bekræfelsesmail sendt til:</strong> @Model.CustomerEmail</li>
        <li><strong>Telefonnummer:</strong> @Model.CustomerPhone</li>
        <li><strong>Udstyr:</strong> @Model.SurfboardAmount stk. @Model.SelectedSurfboard</li>
        <li>
            <strong>Dato:</strong> @Model.RentPeriod.ToString("dd/MM/yyyy")
            @* @if (Model.RentPeriod.Date != Model.RentReturn.Date)
            {
                <span>- @Model.RentReturn.ToString("dd/MM/yyyy")</span>
            } *@
        </li>
        <li><strong>Afhentning:</strong> kl. @Model.RentPeriod.ToShortTimeString()</li>
        <li><strong>Aflevering:</strong> kl. @Model.RentReturn.ToShortTimeString()</li>
        <li><strong>Bemærkninger:</strong> @Model.Remarks</li>
        <li><strong>Samlet pris:</strong> @Model.SelectedPrice</li>
    </ul>

    <p>Husk at medbringe dit bookingnummer ved afhentning. Hvis du har spørgsmål eller brug for at ændre din reservation, er du velkommen til at <a href="Kontakt" style="color: #2e86c1;">kontakte os</a>.</p>

    <p style="text-align: center;">Vi ser frem til at hjælpe dig med at få den bedste surfoplevelse!</p>
</div>


